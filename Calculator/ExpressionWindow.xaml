<Window x:Class="Calculator.ExpressionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Calculator"
        Title="Expression Input"
        Height="200" Width="400"
        WindowStartupLocation="CenterScreen">


    <Window.DataContext>
        <local:Expression/>
    </Window.DataContext>

    <Grid Background="#2D2D30" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding ExpressionStr, UpdateSourceTrigger=PropertyChanged}" 
                 FontSize="14" VerticalAlignment="Center" Margin="5"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5" >
            <Button Content="Calculate" Command="{Binding CalculateCommand}" Width="100" Margin="5"/>

            <Button Content="Close" Command="{Binding CloseCommand}" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
